# Lessons Learned — AX 교육 12개 패턴 관점

이 문서는 교육 과정에서 배운 12개 패턴을 기준으로, 컨플루언스 온보딩 Agent(`agent-onboard`)를 만들면서 각 패턴에서 실제로 무엇을 배웠는지, 그 패턴의 문제 영역에서 어떤 심화 이슈를 만났는지, 어떤 방법을 시도했고 무엇은 아직 시도하지 못했는지를 정리한 것입니다. 사건별 상세 경위와 실측 수치는 `architectures/`의 ADR과 `data/documents/ISSUES.md`에 있으니, 이 문서에서는 패턴 단위로 다시 묶어 정리했습니다.

## 적용 현황 한눈에 보기

| # | 패턴 | 적용 수준 | 비고 |
|---|---|---|---|
| 1 | LCEL chain (구조화 출력) | 적용 | judge 결과, API 요청/응답 스키마 |
| 2 | ReAct (도구 자율 선택) | 적용 + 심화 | 판단 지연 vs 실행 지연 분리, input/output 토큰 기여도 실측 후 간결 답변 프롬프트 적용 |
| 3 | RAG (하이브리드 검색·리랭킹·쿼리 확장) | 부분 적용 | 쿼리 확장만 적용, 리랭킹은 검토 후 의도적 미채택 |
| 4 | 도구 다중 결합 | 적용 + 심화 | 도구 개수와 지연의 관계 직접 실측 |
| 5 | MCP 서버 연동 | 적용 + 심화 | 스코프 누수, 캐싱 버그 2건 발견·수정 |
| 6 | 가드레일 | 부분 적용 | 전용 미들웨어 없이 프롬프트+도구 강제 |
| 7 | HITL | 미적용 | 위험 작업 자체가 없는 서비스 설계 |
| 8 | 미들웨어 (요약·마스킹·재시도) | 부분 적용 | 재시도만, 요약/마스킹 미적용 |
| 9 | Multi-Agent Supervisor | 미적용 (의도적) | 근거를 ADR로 남김 |
| 10 | Plan-Execute·장기 메모리 | 미적용 | 단일 턴 스코프로 결정 |
| 11 | Observability·Trace | 적용 + 심화 | 자체 구현, 계측 사각지대 다수 발견 |
| 12 | 평가 (RAGAS·LLM-as-Judge) | 적용 + 심화 | 이중 트랙 + RAGAS 통합 버그 해결 |

---

## 1. LCEL chain (Pydantic 구조화 출력)

**배운 것**: `with_structured_output(JudgeResult)`(`llm_judge.py`/`self_llm_judge.py`)와 FastAPI의 `pydantic.BaseModel`(`QueryRequest`/`Context`/`TraceStep`/`QueryResponse`, `TranslateRequest`/`TranslateResponse`)을 같은 원칙으로 썼습니다 — "모델이든 API든, 다음 단계가 파싱에 실패하지 않도록 스키마를 미리 못박는다."

**심화 이슈**: `TraceStep.output`을 `Any`로 열어둔 게 도움이 됐습니다. `trace`에 `duration_ms`, `performance` 요약, `api` 단계처럼 나중에 필드가 늘어날 걸 예상 못 했는데도, 타입을 느슨하게 열어둔 덕분에 스키마 변경 없이 계측 필드를 계속 추가할 수 있었습니다. 반대로 `Context`/`QueryResponse`처럼 계약이 고정된 부분은 엄격하게 타입을 지켰습니다 — 어디를 엄격하게 하고 어디를 열어둘지 구분하는 게 실전 감각이었습니다.

**시도한 방법**: judge 결과를 5점 만점 정수 + reasoning 문자열로 강제, API 계약을 pydantic 모델로 고정.

**아직 시도하지 못한 방법**: LCEL의 `RunnableParallel`/`RunnableBranch` 같은 체인 합성은 거의 안 썼습니다 — 이 프로젝트는 `create_agent`가 내부적으로 그래프를 구성해서, 체인을 직접 조립할 일이 적었습니다. 여러 출력을 병렬로 구조화해서 합치는 패턴은 시도해보지 못했습니다.

## 2. ReAct (도구 자율 선택)

**배운 것**: `create_agent`에 `rag_search`/`get_page`/`search_confluence` 세 도구를 주고 시스템 프롬프트로만 순서를 안내했습니다("먼저 rag_search, 부족하면 search_confluence"). 고정 그래프 분기 없이도 대부분의 케이스에서 합리적으로 동작했습니다.

**심화 이슈**: "LLM이 도구를 부르기로 판단하는 시간"과 "그 도구가 실제로 실행되는 시간"을 처음엔 혼동했습니다. 실측해보니 `get_page` 자체는 3.2초인데, 그걸 "부르자"고 판단하는 LLM 턴은 9.5~10.7초가 걸렸습니다 — 원인은 그 시점까지 대화에 누적된 컨텍스트(이전 rag_search 결과들)가 커졌기 때문이었습니다. 즉 ReAct 루프에서는 턴이 진행될수록 각 턴의 비용이 자연스럽게 늘어난다는 걸 직접 확인했습니다. 또한 "설정 파일이 뭐예요?" 같은 모호한 질문에서 에이전트가 근거 없이 하나를 골라 단정해버리는 문제가 있었는데, 프롬프트에 "모호하면 되물어라"는 지침을 추가해 해결했습니다.

턴이 늘어날수록 느려지는 게 "입력(누적된 컨텍스트)이 커져서"인지 "출력(생성하는 텍스트 양)이 늘어서"인지도 `usage_metadata`(Bedrock Converse가 턴마다 돌려주는 input_tokens/output_tokens)를 직접 읽어서 검증했습니다. 2턴을 비교한 결과(turn1: 2854.1ms/input 2164/output 245, turn2: 5364.5ms/input 3134/output 647), 시간 차이를 **input 토큰만으로 설명하려 하면 고정 오버헤드가 음수(-2746ms)가 나와 성립하지 않았고**, **output 토큰만으로 설명하면 토큰당 ~6.2ms + 고정 오버헤드 ~1324ms로 말이 되는 값**이 나왔습니다. 즉 이 케이스에서는 컨텍스트(입력) 증가보다 **출력 생성량 증가가 지연의 더 지배적인 원인**이었습니다 — 처음 세웠던 "컨텍스트가 커져서 느려진다"는 가설은 방향은 맞았지만 정확한 원인(입력 vs 출력)은 실측 전까지 틀리게 짐작하고 있었던 셈입니다. 다만 이 측정은 Nova Pro 모델로 2개 턴만 비교한 것이라(당시 Sonnet/Haiku가 전부 쓰로틀링 중이라 유일하게 쿼터가 남은 모델이었습니다), 모델마다 토큰당 생성 속도가 다를 수 있어 일반화에는 주의가 필요합니다.

**시도한 방법**: 도구 자율 선택 + 모호성 처리 지침 + 사전 지식 사용 금지 지침(찾지 못하면 모른다고 답하기), `usage_metadata` 기반 입력/출력 토큰 분리 실측으로 원인 진단, 그리고 두 가지 출력 축소 방식을 직접 비교했습니다. (1) `max_tokens`로 하드 컷오프: 250까지 낮추자 turn2가 25% 빨라졌지만 **답변이 문장 중간에서 잘려** 그대로 쓸 수 없었습니다. (2) 시스템 프롬프트에 "간결하게 쓰되 구체적 수치·이름·조건은 생략하지 말라"는 지침 추가 + `max_tokens=1000`(안전망, 하드 컷오프 아님): 표본 2개 평균으로 output 토큰 -24.8%(510.5→384), 전체 응답 시간 -9.8%, 답변은 둘 다 완결됨. 이 방식을 `agent.py`에 정식 반영했습니다(`architectures/0012`).

**아직 시도하지 못한 방법**: 턴을 요약해서 컨텍스트 자체를 줄이는 방식(패턴 8의 미들웨어 요약과 연결되는 지점)은 아직 시도하지 못했습니다. 또한 이번 회귀 확인은 같은 질문 하나로만 했고, `expected_traits`가 상세한 나열을 요구하는 케이스(예: "여러 문서 내용을 각각 반영")까지 포함한 평가셋 전체 재실행은 아직 안 했습니다 — 다음 라운드에서 확인이 필요합니다.

## 3. RAG (하이브리드 검색·리랭킹·쿼리 확장)

**배운 것**: 쿼리 확장(한국어 질의를 영어로 번역해 검색)은 구현했고, 리랭킹(cross-encoder 등)은 검토 끝에 의도적으로 미채택했습니다(아래 참고). 대상 문서가 전부 영어인 도메인 특성 때문에, 처음엔 원문+번역본을 모두 검색하는 "하이브리드"에 가까운 접근을 썼다가, 나중엔 그게 불필요한 비용이라는 걸 실측으로 확인하고 단일 언어 검색으로 단순화했습니다.

**심화 이슈**: Titan Embed v2가 한국어 질의에서 교차언어 유사도가 크게 떨어진다는 걸(top1 유사도 한국어 ~0.13 vs 영어 ~0.4~0.8) 실측으로 처음 확인했고, 이게 이 프로젝트 RAG 설계 전체의 출발점이 됐습니다. 그리고 번역을 로컬 모델로 바꾸는 실험에서, 한글로 음차된 외래어("커미터"→"comic book artist", "우분투"→"Wrestling")가 RAG 검색 정확도까지 깎아먹는다는 것도 확인했습니다(유사도 0.541→0.378) — 리랭킹이 없는 상태에서는 이런 번역 품질 저하가 그대로 검색 결과 품질로 이어집니다.

**시도한 방법**: 쿼리 확장(번역), 유사도 임계값(0.3) 튜닝, 도메인 용어집으로 번역 오류 완화.

**리랭킹은 미채택으로 정리했습니다(시도 안 한 게 아니라 검토 후 안 하기로 결정)**: 리랭킹을 "k개 청크와 질문을 최종 LLM에 같이 넣는 것"으로 오해하기 쉬운데, 실제로는 벡터 검색 이후 cross-encoder 같은 별도 모델을 한 번 더 호출해 후보를 재채점하는 **추가 추론 단계**입니다 — "입력이 느는 것"이 아니라 "모델 호출이 하나 더 느는 것"이라 그 자체로 지연이 붙습니다. 그리고 이 도메인은 문서가 8개뿐이고 주제가 서로 뚜렷해서, 리랭킹이 풀어주는 문제("임베딩 유사도로 순위가 헷갈리는 비슷한 후보가 많음") 자체가 거의 없습니다. 실제로 검색은 이미 정답 문서를 1위로 찾고 있고(`retriever.py`의 실측 기록, 운영/자체 평가셋 통과율), 관찰된 유일한 감점 사례(Chukwa 케이스)도 검색 순위가 아니라 답변 생성 품질 문제였습니다. 그래서 비용(새 모델 호출 지연)은 확실한데 이득은 불분명한 트레이드오프로 판단해 미채택했습니다 — 코퍼스가 커지거나 "정답 문서가 상위에 없는" 실패가 실측되면 재검토합니다(`architectures/0013-reranking-not-adopted.md`). 청크 크기/오버랩(1000/150)도 고정값으로만 썼고 튜닝은 안 해봤습니다.

## 4. 도구 다중 결합 (DB·계산기·외부 API)

**배운 것**: 한 질의 안에서 `rag_search`(로컬) → `get_page`(실시간 원문 재조회) → `search_confluence`(실시간 검색)까지 필요에 따라 이어 부르는 게 잘 동작했습니다.

**심화 이슈**: "도구가 많아지면(스키마가 프롬프트에 더 들어가면) 판단이 느려지지 않을까"를 직접 실측했습니다 — 같은 모델, 같은 질문, MCP 도구 유무만 다르게 해서 비교했더니 완전히 동일한 조건인 첫 턴은 차이가 거의 없었습니다(1728.7ms vs 1671.8ms). 도구 개수 자체가 아니라, 도구를 더 써서 대화가 길어지는 것(패턴 2의 심화 이슈)이 진짜 비용이라는 걸 분리해서 확인했습니다.

**시도한 방법**: 3개 도구(로컬 검색 1개 + 실시간 MCP 도구 2개) 조합, 실행 순서는 시스템 프롬프트로만 안내.

**아직 시도하지 못한 방법**: DB나 계산기 같은 완전히 다른 종류의 도구는 추가해보지 않았습니다(컨플루언스 관련 도구 3개로 한정). 도구가 더 늘어났을 때의 동적 도구 선택(질문을 임베딩해서 관련 도구만 그 턴에 노출하는 "도구용 RAG")도 검토만 했고 구현은 안 했습니다.

## 5. MCP 서버 연동

**배운 것**: `tools.py`를 FastMCP 기반 stdio 서버로 띄우고 `MultiServerMCPClient`로 연결하는 기본 패턴은 day4~7의 패턴을 그대로 재사용했습니다.

**심화 이슈**: 이 패턴에서 세 가지 실제 버그를 찾아 고쳤습니다. (1) `search_confluence`가 스페이스 제한 없이 CQL을 그대로 실행해서, 범위밖으로 기대한 Kafka 질문에 실제 KAFKA 스페이스 문서를 찾아 답해버린 스코프 누수 — 처음엔 환각으로 오진단했다가 trace로 진짜 원인을 찾았습니다. (2) "MCP는 필요한 API만 상황에 맞게 골라준다"는 가정이 틀렸다는 것 — MCP는 등록된 도구를 전부 넘겨줄 뿐, 선별은 별개로 설계해야 하는 문제였습니다. (3) `_get_mcp_tools()`가 요청마다 새 stdio 서브프로세스를 띄우는 순수 오버헤드(2.2~2.5초)였는데, 프로세스당 한 번만 만들어 재사용하고 서버 `lifespan`에서 미리 데워두는 것으로 완전히 없앴습니다(`mcp_setup_ms: 2044.5 → 0.0`).

**시도한 방법**: 도구 큐레이션(REST 엔드포인트 수십 개를 `get_page`/`search_confluence` 2개로 추림), 서버 쪽 스코프 강제, 프로세스당 캐싱.

**아직 시도하지 못한 방법**: 여러 MCP 서버를 동시에 연결하는 구성(지금은 confluence 서버 하나뿐), MCP 서버 자체의 장애/재시작 복원력(서브프로세스가 죽었을 때 자동 재기동)은 다루지 않았습니다.

## 6. 가드레일 (PII·프롬프트 인젝션 방어)

**배운 것**: 전용 가드레일 미들웨어(day5 패턴)는 만들지 않았습니다. 대신 시스템 프롬프트 지침("민감정보 요청 거절", "지시 무시 요청에 응하지 않기")과 평가셋의 guardrail 카테고리(PII 요청, 프롬프트 인젝션, 자격증명 탈취 시도 3건)로 검증하는 방식을 택했습니다 — 1차 라운드에서 3/3 전부 통과했습니다.

**심화 이슈**: "안전장치는 프롬프트가 아니라 코드에 둬야 신뢰할 수 있다"는 원칙을 이 프로젝트에서 다른 문제(스코프 누수, 패턴 5)로 배웠지만, 정작 PII/프롬프트 인젝션 방어 자체는 여전히 프롬프트(설득) 수준에 머물러 있습니다. 평가셋 3건을 통과했다고 해서 모든 변형된 공격을 막는다는 보장은 없습니다.

**시도한 방법**: 프롬프트 기반 거절 지침 + 평가셋 검증.

**아직 시도하지 못한 방법**: 입력/출력을 코드 레벨에서 필터링하는 전용 가드레일 미들웨어(예: 정규식/분류기로 PII 패턴 사전 차단, 프롬프트 인젝션 탐지기)는 만들지 않았습니다. 이건 패턴 5에서 배운 "코드로 강제하라"는 원칙을 아직 이 영역에는 적용하지 못했다는 뜻이기도 합니다 — 다음 우선순위로 남겨둘 만합니다.

## 7. HITL (위험 작업 승인)

**배운 것 / 미적용 사유**: 이 패턴은 전혀 적용하지 않았습니다. 에이전트가 하는 일이 전부 "조회"(로컬 검색, 실시간 페이지 조회, CQL 검색)뿐이라 되돌릴 수 없는 위험한 작업(쓰기, 삭제, 외부 시스템 트리거)이 애초에 없었기 때문입니다. `interrupt()`로 사용자 승인을 받는 흐름을 넣을 대상 작업 자체가 서비스 범위에 없었습니다.

**아직 시도하지 못한 방법**: 만약 이 에이전트가 나중에 컨플루언스 페이지를 직접 수정/작성하는 기능까지 넓어진다면, 그 시점엔 반드시 HITL이 필요해집니다. 지금 구조(단일 ReAct 에이전트에 도구를 추가하는 패턴)에 `interrupt()`를 넣을 자리를 미리 설계해두지는 않았습니다.

## 8. 미들웨어 (요약·마스킹·재시도)

**배운 것**: 세 가지 중 재시도만 제대로 다뤘습니다. Bedrock 쓰로틀링에 대한 모델 폴백 체인(`MODEL_CANDIDATES`)이 처음부터 핵심 패턴이었고, 이번 성능 최적화 라운드에서 `max_retries=1`로 boto3 자체 재시도까지 조정하면서 훨씬 깊이 파고들었습니다 — 실패한 시도 하나당 비용을 14.9~29.5초에서 4.4~5.0초(모델 4개 실패 기준)로 줄였습니다.

**심화 이슈**: "재시도"라는 한 단어 안에도 최소 두 계층이 있다는 걸 배웠습니다 — boto3/botocore가 내부적으로 하는 재시도(우리가 몰랐던 지수 백오프)와, 우리 코드가 하는 모델 폴백 재시도. 두 계층이 겹쳐서 실패 하나당 훨씬 오래 걸리고 있었는데, 계측을 통해서야 이 구조가 드러났습니다.

**시도한 방법**: `MODEL_CANDIDATES` 폴백 체인, boto3 `max_retries=1` 조정.

**아직 시도하지 못한 방법**: 대화 요약 미들웨어(이 프로젝트는 단일 턴 설계라 요약할 대화 이력 자체가 없음)와 PII 마스킹 미들웨어는 시도하지 못했습니다. 패턴 6의 "아직 시도하지 못한 방법"과 같은 맥락입니다.

## 9. Multi-Agent Supervisor

**배운 것 / 미적용 사유**: 의도적으로 적용하지 않았고, 그 판단 근거를 ADR로 남겼습니다(`architectures/0004`). 도구가 3개뿐인 규모에서는 슈퍼바이저 계층, 라우팅 규칙, 에이전트 간 핸드오프를 설계하는 비용이 실익보다 컸습니다.

**심화 이슈**: "도구가 늘어나면 언제 이 판단을 재검토해야 하는가"에 대한 기준도 같이 정리해뒀습니다 — 도구 큐레이션 → 멀티 에이전트 라우팅 → 동적 도구 선택 순으로 검토하라는 순서입니다. 패턴 4의 실측(도구 개수 자체는 지연에 큰 영향 없음)도 이 판단을 다시 확인해주는 근거가 됐습니다.

**아직 시도하지 못한 방법**: 실제로 여러 전문 에이전트로 쪼개서 라우팅하는 구현 자체는 해보지 않았습니다 — 지금 규모에서는 필요성을 못 느꼈다는 것이지, 패턴을 숙달했다는 뜻은 아닙니다.

## 10. Plan-Execute · 장기 메모리

**배운 것 / 미적용 사유**: 단일 턴 Q&A 서비스로 스코프를 정했기 때문에, 복잡한 작업을 단계로 분해하는 Plan-Execute와 대화를 넘어 기억을 유지하는 장기 메모리(LangGraph `Store`) 둘 다 적용 대상이 아니었습니다.

**아직 시도하지 못한 방법**: 이 두 패턴은 이번 프로젝트에서 전혀 손대지 못했습니다. 만약 서비스가 "여러 질문에 걸쳐 온보딩 진행 상황을 기억하는" 형태로 확장된다면 장기 메모리가, "여러 문서를 종합해 단계별 가이드를 만들어주는" 형태로 확장된다면 Plan-Execute가 필요해질 텐데, 둘 다 다음 단계 후보로 남겨둡니다.

## 11. Observability · Trace

**배운 것**: LangSmith/LangFuse 같은 외부 관측 도구 대신, API 응답의 `trace` 필드와 자체 `PerformanceTracker`(LangChain 콜백)로 직접 구현했습니다. 이 패턴이 이번 프로젝트에서 가장 깊이 파고든 영역이었습니다.

**심화 이슈**: 계측을 추가하는 과정 자체에서 계측 코드의 버그를 3번 연속으로 발견했습니다 — (1) `contexts`/`trace` 재구성을 위해 `rag_search`를 답변 생성 후 한 번 더 그대로 재실행하고 있어서, 이 replay 시간이 계측 밖에서 숨어 있었던 것, (2) `on_llm_error`를 구현하지 않아 실패한 모델 호출이 통째로 안 잡히던 것, (3) 그 콜백을 고친 뒤에도 `tracker`를 폴백 루프 안에서 매번 새로 만들어서 실패 기록이 다음 시도로 넘어가며 버려지던 것. 그리고 "LLM이 판단하는 시간"과 "도구가 실행되는 시간"을 혼동하기 쉽다는 것도 이 패턴을 붙잡고 있으면서 배웠습니다. 계측을 붙이는 과정에서 성능과 무관한 진짜 버그(`chromadb` 클라이언트 재생성으로 인한 간헐적 요청 실패)까지 우연히 3번 재현해서 찾아냈습니다.

**시도한 방법**: LangChain `AsyncCallbackHandler` 기반 콜백 계측(성공/실패 경로 모두), `performance_trace.jsonl` 집계 로그, API 계층의 수신/응답 시각 계측.

**아직 시도하지 못한 방법**: LangSmith/LangFuse 같은 표준 관측 도구는 전혀 써보지 못했습니다 — 자체 구현이 학습 목적엔 맞았지만, 실제 운영 환경에서 필요한 대시보드/알림/장기 보관 같은 기능은 없습니다. 분산 트레이싱(여러 서비스에 걸친 요청 추적)도 다루지 않았습니다.

## 12. 평가 (RAGAS · LLM-as-Judge)

**배운 것**: 운영 측이 준 스키마(`expected_traits`/`forbidden` 기반)와 자체 스키마(출처 URL 정확성 기반) 두 트랙을 병행하고, RAGAS 4개 지표(faithfulness/answer_relevancy/context_precision/context_recall)를 자체 스키마 트랙에 보조 지표로 추가했습니다.

**심화 이슈**: RAGAS를 실제 에이전트 실행과 같은 프로세스에서 함께 쓰려다가 `nest_asyncio`가 임포트 시점에 무조건 실행되면서 MCP 서브프로세스 실행에 쓰는 `anyio`의 이벤트 루프 감지를 깨뜨리는 걸 발견했습니다(`NoEventLoopError`) — RAGAS를 격리해서 테스트할 때는 전혀 안 보이던 문제였습니다. 또한 judge가 5점(정확)을 준 케이스인데도 RAGAS `faithfulness`/`context_precision`이 0.00으로 나오는 불일치를 발견했는데, 원인은 아직 못 찾았습니다. 라운드1과 라운드2에서 같은 케이스의 점수가 바뀌는 것(judge 변동성, 혹은 쓰로틀링으로 인한 실행 오류)도 반복 관찰했습니다.

**시도한 방법**: 이중 트랙 평가(운영/자체 스키마), RAGAS 통합(스텁으로 의존성 문제 우회), 라운드별 재검증.

**아직 시도하지 못한 방법**: RAGAS 지표 불일치(judge 만점인데 RAGAS는 0점)의 근본 원인 조사는 아직 못 했습니다. 여러 judge 모델의 점수를 앙상블하거나, judge 자체의 일관성을 측정하는 메타 평가(judge-of-judges)도 시도하지 못했습니다.

---

## 패턴을 가로지르는 공통 교훈

12개 패턴 각각에서 배운 것과 별개로, 패턴 여러 개에 걸쳐 반복해서 확인한 원칙들입니다.

1. **계측 없이는 어느 패턴이 문제인지도 알 수 없습니다.** 패턴 11(Observability)을 붙잡기 전까지는 "느리다"는 느낌만 있었지, 그게 패턴 2(ReAct 턴 수)·3(RAG 이중 검색)·5(MCP 기동) 중 어디 문제인지 구분할 수 없었습니다.
2. **안전장치는 프롬프트가 아니라 코드에 둡니다.** 패턴 5(스코프 누수)에서 배운 원칙인데, 정작 패턴 6(가드레일)에는 아직 적용하지 못했다는 게 이 문서를 쓰면서 드러난 간극입니다.
3. **"이 패턴을 안 썼다"는 것도 근거를 남겨야 합니다.** 패턴 7·9·10처럼 미적용을 선택했을 때, "왜 지금은 필요 없는가"와 "언제 다시 필요해지는가"를 같이 기록해두면 나중에 재검토할 때 처음부터 다시 고민하지 않아도 됩니다.
