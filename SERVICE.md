# SERVICE · 컨플루언스 온보딩 Agent

## 1. 사용자·문제·가치
- **누구를 위한 서비스인가요**: SI/SM 프로젝트에 새로 투입되는 신입 개발자분들을 위한 서비스입니다. 연간/분기 기준 약 10~50명 규모를 가정하고 있고, PoC 단계에서는 프로젝트마다 개별 인스턴스로 운영할 계획입니다.
- **어떤 문제를 풀고 싶었나요**: 신입분들이 컨플루언스에 흩어진 온보딩 정보(방화벽/보안 설정, 사내 시스템 가입, 개발환경 구축 가이드 등)를 찾지 못해 겪는 비효율을 풀어드리고 싶었습니다.
- **이 에이전트가 없을 때는 어떻게 해결하고 계셨나요**: 막히면 사수님이나 멘토님께 메신저로 여쭤보는 방식이었습니다. 응답 자체는 빠르지만(체감 5분 이내), 진짜 문제는 속도가 아니라 매번 다른 분의 업무를 끊어야 하고 셀프서비스가 안 된다는 점이었습니다. 셀프서비스로 직접 찾아보시더라도 긴 문서를 처음부터 끝까지 읽어야 하고, 애초에 뭐부터 봐야 할지도 모르는 상황이었습니다.
- **이걸로 뭐가 좋아지나요**: 신입분들이 스스로 답을 찾으실 수 있어서 (1) 온보딩 소요 시간이 줄어들고 (2) 사수님의 인터럽트가 줄어들고 (3) 에이전트가 못 찾은 질문을 로그로 남겨 문서 품질 개선을 유도할 수 있고 (4) 설정 누락 같은 투입 초기 리스크도 줄어들 것으로 기대합니다.

## 2. 서비스 확장 관점
- **사내에 어떤 기여를 할 수 있나요**: 직접적인 수익화 대상은 아닙니다(삼성SDS 내부 전용 생산성 도구이고, 고객사 납품이나 상품화는 이번 범위 밖입니다). 가치는 "온보딩 시간 단축 → 투입 인력의 생산 가능 시간 증가 → 비용 절감"이라는 ROI로 설명드릴 수 있을 것 같습니다.
- **규모는 어느 정도로 보고 있나요**: 연간 10~50명 정도를 생각하고 있습니다. PoC는 프로젝트별 개별 인스턴스로 운영하고, 효과가 검증되면 전사 공유 서비스로 확장하는 2단계 전략을 그려봤습니다.
- **대체제와는 어떻게 다른가요**: 컨플루언스는 자체 AI 기능(Atlassian Intelligence/Rovo)을 이미 제공하고 있습니다. 그럼에도 직접 만들어보려는 이유는 다음과 같습니다. (1) Rovo는 Atlassian Cloud 전용이라 사내 컨플루언스가 온프레미스(Data Center)라면 애초에 쓰지 못할 수도 있습니다. (2) "모르면 모른다"고 답하는 것 같은 온보딩 특화 로직과 감사 가능한 trace를 저희가 직접 설계할 수 있습니다. (3) 이미 확보된 사내 Bedrock 계정으로 비용을 직접 통제할 수 있습니다. (4) MCP 구조라 컨플루언스 외의 다른 사내 시스템까지 확장하기 수월합니다. 더 자세한 내용은 `data/documents/ISSUES.md` 3절과 `data/documents/SERVICE.md` 2절에 정리해뒀습니다.
- **다음 단계로 어떤 확장을 생각하고 계신가요**: 지라(Jira) MCP 연동을 검토하고 있습니다. 같은 프로젝트의 관련 업무(진행 중인 이슈, 담당자, 최근 활동)를 컨플루언스 문서와 함께 찾아드리면, 신입분들이 "무엇을 해야 하는지"뿐 아니라 "지금 무엇이 진행되고 있는지"까지 파악하실 수 있어 실전 투입이 더 빨라질 것으로 기대합니다. 이 확장은 저희가 배운 패턴 중 세 가지를 실제로 적용해볼 좋은 기회이기도 합니다.
  - **도구 다중 결합**: 지금은 컨플루언스 관련 도구 3개(`rag_search`/`get_page`/`search_confluence`)만 결합돼 있는데, 지라 도구(이슈 검색/조회)까지 더해지면 한 질문 안에서 여러 시스템을 넘나들며 답을 찾는 상황이 처음 생깁니다.
  - **MCP 서버 연동**: 기존 `src/agent/tools.py`(Confluence MCP 서버)와 같은 패턴으로 `jira-tools`라는 별도 MCP 서버를 하나 더 두는 구조를 생각하고 있습니다. 도구가 2개 규모일 때는 MCP의 표준화·프로세스 격리 이점이 크게 체감되지 않았는데(`data/documents/ISSUES.md` 3절), 도메인이 하나 더 늘면 이 이점이 실제로 드러날 수 있는 첫 사례가 될 것 같습니다.
  - **Multi-Agent Supervisor**: 컨플루언스(문서 검색)와 지라(이슈 조회)는 성격이 다른 두 도메인이라, 지금처럼 단일 에이전트에 도구를 다 몰아넣기보다 두 개의 전문 에이전트(예: `confluence_agent`, `jira_agent`)를 슈퍼바이저가 질문 성격에 따라 라우팅하는 구조를 재검토해볼 만합니다. 저희가 처음에 슈퍼바이저를 채택하지 않았던 이유(`architectures/0004-single-agent-not-supervisor.md`)가 "도구가 3개뿐이라 실익이 없다"는 것이었는데, 이 확장이 그 재검토 기준(도구가 늘고 성격이 갈릴 때)에 해당하는지 실제로 검증해볼 좋은 케이스입니다.

  다만 이건 아직 구체적인 설계·구현 전 단계의 기획 아이디어입니다. 실제로 진행하려면 지라 프로젝트/스페이스 범위를 어떻게 제한할지(컨플루언스 스코프 누수 사례처럼, `architectures/0005-mcp-tool-scope-enforcement.md`의 교훈을 그대로 적용해야 합니다), 인증 방식, 그리고 도구가 도메인 2개로 늘어나는 것만으로 슈퍼바이저가 필요한 규모인지부터 먼저 확인이 필요합니다.

## 3. 사용 예상 도구·데이터
- **도구 목록**
  - `rag_search` — 사전에 크롤링·임베딩해둔 로컬 문서를 벡터 검색합니다. 한국어/영어 질의를 모두 번역해 이중으로 검색한 뒤 유사도가 더 높은 쪽을 채택합니다.
  - `get_page` (MCP) — 특정 페이지를 URL 또는 ID로 실시간 재조회합니다.
  - `search_confluence` (MCP) — CQL 기반으로 실시간 검색합니다. `CONFLUENCE_SPACE_KEY`(기본값 `HADOOP2`)로 스페이스를 서버 쪽에서 강제로 제한해서, 관련 없는 다른 프로젝트 문서까지 검색되는 걸 막았습니다.
- **데이터 소스**: Apache Hadoop의 `HADOOP2` 컨플루언스 공간(cwiki.apache.org)을 쓰고 있습니다 — **실제 공개 데이터이고, 더미 데이터는 아닙니다.** 사람이 직접 고른 URL 리스트(현재 8개이고, 20~30개로 늘려갈 계획입니다)를 스크립트로 수집하고, 첨부 이미지(다이어그램 등)는 저비용 비전 모델로 분류·텍스트화해서 함께 인덱싱하고 있습니다. 실무 컨플루언스로 교체하실 때는 `CONFLUENCE_BASE_URL`/`CONFLUENCE_AUTH_TOKEN`/`CONFLUENCE_SPACE_KEY`(`.env`)만 바꾸시면 됩니다.

## 4. 서비스 정책 (가드레일 요약)
1. **근거 없이는 답하지 않습니다**: `rag_search`/`search_confluence`/`get_page`로 실제로 찾은 내용에만 근거해서 답하도록 했습니다. 검색 결과에 근거가 없으면, 이미 잘 알려진 주제라 하더라도 사전 지식으로 채우지 않고 "찾지 못했습니다"라고 정직하게 답하도록 했습니다.
2. **대상 스페이스를 벗어나지 않습니다**: 실시간 검색은 항상 지정된 컨플루언스 스페이스로 범위를 제한하도록 했습니다.
3. **정책 위반 요청은 거절합니다**: 자격증명/비밀번호 요청, PII 요청, 프롬프트 인젝션 시도가 들어오면 요청을 거절하고, 민감 정보나 시스템 프롬프트를 노출하지 않도록 했습니다.
4. **모호한 질문은 단정하지 않습니다**: 질문이 짧거나 여러 의미로 해석될 수 있을 때는, 하나로 단정하지 않고 모호하다는 점을 먼저 말씀드린 뒤 후보를 제시하며 되묻도록 했습니다.
5. **답변 언어는 고정해뒀습니다**: 질문이 한국어든 영어든 답변은 항상 한국어로 드리고, 출처 인용구만 원문(영어) 그대로 남기도록 했습니다.

세부 케이스는 `evaluation/test_queries.csv`의 guardrail 카테고리(3건: 자격증명 요청, 프롬프트 인젝션, PII 요청)에서 검증하고 있습니다 — 1차 라운드에서는 3건 모두 통과했습니다.

## 5. 성공 기준
- **최소 통과율**: `evaluation/test_queries.csv` 20건(positive 8·negative 4·edge 5·guardrail 3) 중 **70% 이상** 통과하면 완성으로 보려고 합니다. 케이스 하나의 통과 조건은 "LLM-judge 5점 만점 중 4점 이상"이면서 **동시에** "`expected_tools`에 지정된 도구가 실제로 호출됨"입니다.
- **실측치**: 1차 라운드(`evaluation/round1_report.md`)에서 18/20(90%)을 통과했습니다. 실패했던 2건 중 1건(모호한 질문 처리)은 시스템 프롬프트를 보강해서 재검증까지 통과시켰고, 나머지 1건은 재검증에서 통과해서 판정 변동성으로 판단했습니다. 2차 라운드(`evaluation/round2_report.md`)는 이 개선을 반영해서 다시 실행해봤습니다.
- **"쓸만하다"는 어떻게 판단할까요**: (1) 20건 통과율 70% 이상인지 (2) guardrail 3건이 전부 통과했는지 (3) 출처 URL이 실제 문서와 일치하는지(LLM-judge rubric에서 가장 중요하게 보는 기준입니다)로 보려고 합니다. RAGAS(context_recall/precision, faithfulness, answer_relevancy)는 아직 도입하지 못해서 정량 지표는 LLM-as-Judge에만 의존하고 있습니다 — 이 부분은 `README.md`의 "RAGAS 평가 결과" 절에 솔직하게 밝혀뒀습니다.
