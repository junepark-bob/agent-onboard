# 컨플루언스 온보딩 Agent - 의존성 목록
# 루트 .venv 에서 설치: pip install -r requirements.txt (repo 루트) 후 pip install -r mini-pjt/requirements.txt
# 버전은 이 프로젝트에서 실제로 동작을 검증한 조합으로 고정했다.

# 공통 (모든 파트)
python-dotenv==1.2.3
boto3==1.43.91

# 에이전트 (src/agent.py) - Bedrock 모델 + LangGraph 기반 create_agent
langchain==1.4.0
langchain-aws==1.7.5
langgraph==1.2.11

# RAG (src/retriever.py) - 임베딩 + 벡터 검색
langchain-text-splitters==1.1.2
langchain-chroma==1.1.0
chromadb==1.5.9

# MCP (src/tools.py, src/agent.py) - Confluence 도구 서버 및 연결
mcp==1.30.0
fastmcp==2.13.0
langchain-mcp-adapters==0.3.2

# 크롤링 (crawl_confluence.py) - Confluence REST API 호출 + HTML -> 마크다운 변환 + 이미지 설명 생성
requests==2.34.2
markdownify==1.2.3
Pillow==12.3.0

# 서버 (server.py) - FastAPI
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
